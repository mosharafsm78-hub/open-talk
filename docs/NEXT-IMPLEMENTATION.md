# Open Talk implementation plan

The next implementation layer is intentionally backend-first: authenticated Supabase data, realtime matching, WebRTC signaling/TURN, consent-aware transcription, AI assessment, persistent improvement tracking, moderation, notifications, analytics, and Android packaging against the same API.

External provider projects and credentials must be created/authorized by the owner before they can be provisioned or deployed. Never commit provider secrets to this repository.
