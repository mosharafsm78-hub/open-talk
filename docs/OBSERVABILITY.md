# Observability

Track service health, API latency/error rates, match success rate, WebRTC connection failures, conversation completion rate, AI processing failures, moderation queue volume and crash-free sessions.

Never put transcript/audio contents or authentication tokens into ordinary application logs. Use correlation IDs and structured operational events. Alert on sustained failures rather than individual transient errors.